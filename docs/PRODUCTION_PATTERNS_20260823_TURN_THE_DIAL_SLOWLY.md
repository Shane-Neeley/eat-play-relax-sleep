# Production patterns — Turn the Dial Slowly (2026-08-23)

This run tested a smooth compound-meter lane that had not appeared in the recent daily catalog: 108 BPM 6/8, a guitar-friendly pluck/bass pocket, one true kick subtraction, a whole-step middle turn, and a return that resolves earlier than the opening phrase. The arrangement passed the deterministic creative checks at 9/9; the unfamiliar meter required and received an explicit human listening approval.

The visual method also changed lanes. Instead of the recent basin marbles, semaphore fins, rail corrections, blooms, suspended weights, or prism bands, a Pillow/FFmpeg renderer draws four luminous instrument dials on a blue-black stage. They enter one at a time, reduce to a single dial for the subtraction, turn upward, and settle on one diagonal. The truthful thumbnail uses the all-dial frame with no text.

The run's frontier lead came from the official Sonic Pi 4.6 release: nested `tuplets` timing may make compound-meter experiments easier to author. It was preserved as a validated research packet outside the public checkout and deliberately not used in the public render; EPRS BeatScript remained the reproducible release oracle.

The required browser-first Studio preflight visibly confirmed the CashForClankers channel and reached Upload videos, but the file chooser returned `Not allowed` and the one native-picker attempt returned `Computer Use server error -10005: noWindowsAvailable`. One authorized YouTube Data API fallback completed the public upload, metadata correction, custom thumbnail, and English caption upload. Watch-page and thumbnail HTTP checks returned 200; the oEmbed endpoint returned 401 in this environment and is recorded as a limitation rather than hidden.

The final media is deterministic local code/audio/video only: no generative-AI assets, external recordings, stock footage, faces, logos, or private information. Telegram report `2581`, WAV attachment `2582`, and video attachment `2583` all returned `{ok:true}`.
